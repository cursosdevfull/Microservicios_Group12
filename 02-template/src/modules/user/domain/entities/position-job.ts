export class PositionJob {
  area: string;
  position: string;
  year: number;

  constructor(area: string, position: string, year: number) {
    this.area = area;
    this.position = position;
    this.year = year;
  }
}
